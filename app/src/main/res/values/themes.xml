<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.App1" parent="Theme.MaterialComponents">
        <item name="android:windowLightStatusBar">true</item>//change item on status bar color
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.App1" parent="Base.Theme.App1" />
    <style name="MySearchViewStyle" parent="Widget.AppCompat.SearchView">//search view
        <item name="android:textColorHint">@color/black</item>//hint
        <item name="android:editTextColor">@color/black</item>//text that I write it
    </style>
</resources>